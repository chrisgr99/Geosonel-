# Section 27 — Strudel as Pattern Language (superseded — forwarding stub)

Superseded. This section's content has been folded into the current sections during the v2.5 fold-back pass. The section file remains as a forwarding stub; future revisions may remove it entirely once external references (HANDOFF.md, in-source comments, the diary) have been updated.

Where each piece went:

- Background and motivation (GXSTR lineage and the architectural mismatch between Strudel's master cycle and GXW's free-running per-source clock model) → Section 24 (GeoMaestro, GeoSonix, and GXSTR Reference).
- The architectural split (GXW owns the WHEN, Strudel owns the WHAT; Strudel's three separable layers) → Section 1 (Vision) and Section 2 (Conceptual Model) absorbed the framing; the deeper three-layer detail is in Section 12 (Pattern Engine).
- Pattern evaluation flow (queryArc plumbing, two-pass evaluation, firing-context pointer, the residual audio lookahead) → Section 12 (Pattern Engine).
- Curated modifier vocabulary (within-cycle modifiers, cross-cycle for continuous sources, cross-cycle for one-shot sources with soft-warning rule) → Section 10 (Pattern Language).
- Static and dynamic signals (Strudel built-ins; the OKLCh-derived px image-colour signals; sprite kinematic signals; harmony-context signals as placeholders) → Section 10 (Pattern Language). The firing-context pointer mechanism the dynamic signals consume → Section 12 (Pattern Engine).
- Tempo and scaling (master BPM × beatsPerCycle wall-clock derivation, mini-notation handles within-cycle density, GXW does not use Strudel's slow/fast/compress) → Section 7 (Transport and Tempo) for the foundational concepts; the implementation math (T_event derivation, the direct fractional-position-to-wall-clock mapping) lives in Section 12 (Pattern Engine).
- Continuous and one-shot firing (same primitive serves both modes; flourish duration field for one-shot) → Section 12 (Pattern Engine).
- Open questions (tonal integration, per-source flourish duration default, concurrent firings, edit-time pattern preview, sample bank loading, mini-notation autocomplete, Strudel version sync) → Section 25 (Open Questions) as entries 27 through 33. Recording and replay was marked resolved in this section's own text and pruned rather than relocated. Mute and solo became a subsection in Section 12 and is no longer an open question.
- Pre-implementation design tasks (the six items resolved during the design pass) → all resolved. Decisions folded into Section 12 (Pattern Engine) where they shaped the architecture (Pass 2 edge cases, pattern-edit takeover behaviour, cycle counter persistence, audio commit window, Strudel global state initialization); otherwise pruned as historical preamble.
- Audio firing path: full strudel, phased rollout (Phase 1 through Phase 4 plan) → Section 12 (Pattern Engine) for the architectural decisions. The phase-by-phase implementation status is captured in design/TODO.md's component-based view (Pattern firing primitive, Dynamic signals, MIDI output, Canvas rendering).
- One-cycle-ahead scheduling and dynamic-signal late-refresh dispatch → Section 12 (Pattern Engine). The implementation-postmortem detail (commit walker internals, noteOff dispatch subtleties, CC stream specifics) was pruned during fold-back; the architectural decisions retained.
- Implementation order (Tier 1 through Tier 6 plan) → design/TODO.md's component-based view. The Tier framing was retired in favour of organizing work by component, so the absorption was a no-op rather than a verbatim move; the substance is captured under Pattern firing primitive, Inspector, Code tab and behaviors.js, Dynamic signals, and Canvas rendering.
