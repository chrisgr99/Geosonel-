# Section 28 — Pattern Authoring and the Cursor Model

Superseded. This section's content has been folded into the current sections during the v2.5 fold-back pass. The section file remains as a forwarding stub; future revisions may remove it entirely once external references (HANDOFF.md, in-source comments, the diary) have been updated.

Where each piece went:

- Object identifiers (SPR/TRG/CRV three-letter-plus-integer scheme, idCounters sub-object in scene.json, fill-pass behaviour, conventional shape regex, new-score template counter seeding) → Section 3 (Scene Structure). The callback function naming convention (slotName_objectId) and the labelled-statement form ($objectId: expression) extension to the same convention → Section 9 (Behaviour Slots and behaviors.js).
- Inspector integration (the three-band layout, Band 1 cycle-duration and pattern rows, Band 2 Starting State row absorbing position and velocity, Band 3 callback-slot rows, greying rules per kind, Hide Cursor as the UI-only rename of the mute field) → Section 13 (User Interface).
- The Code tab as the home for pattern editing (labelled-statement form, scene-load Acorn pre-processing that keeps labelled blocks inert at load time, scene-runtime-cache separation, active-block highlight via cmTheme.js accent green, two-surface validation rejecting live-parsing, bidirectional navigation between Properties and Code tabs, untagged-blocks handling) → Section 9 (Behaviour Slots and behaviors.js).
- Playback control during authoring (Spacebar with text-input exception, Cmd-Period as the always-active alternative, double-click on empty canvas background, engine-load auto-trigger on first user interaction, first-gesture playback gate) → Section 13 (User Interface).
- Cursors, marker layout, and the cursor-as-collider model → distributed across the relevant per-kind and behaviour sections:
  - Section 4 (Curves): cursor extents schema, the toolbar-creation default of cursorR=1 and cursorL=1, vx/vy velocity fields, marker re-rendering on cycle wrap, stochastic-versus-deterministic distinction.
  - Section 6 (Sprites): cursor extents schema, cursor persistence when stationary, default horizontal direction for never-moved sprites, the distinction that sprite cyclePattern firing happens at the sprite's current position rather than at fixed markers along a path.
  - Section 8 (Collision Model): the cursor-as-collider rationale (why requiring a cursor for collision dissolves the point-vs-point degeneracy), mute and external collisions (mute silences self-firing only; external collisions credit colliding source's hasHit and muted source's beenHit), pattern firing through markers (what happens to audio when a curve has cyclePattern but no cursor).
  - Section 22 (Sprite Physics Details): curve velocity is physics-integrated; vx/vy translation per simulation tick with the same inside-only canvas-boundary reflection as sprite motion; bbox-versus-per-shape geometric test equivalence for the current line/ellipse/piste shape types.
- Visualisation (static glyphs at edit time for deterministic patterns, after-the-fact breadcrumbs for stochastic ones, hybrid mode with parse-time detection) → Section 13.5 (Canvas) under the Pattern-marker visualisation subsection. The broader Canvas consolidation pass folding sections 13, 21, 22, and 26 plus new inspector-tab content remains a separate later task.
- Deferred items (sprite trajectory prediction for marker placement, sprite-vs-sprite collision) → Section 25 (Open Questions) as entries 34 and 35.
